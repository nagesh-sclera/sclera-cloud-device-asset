package org.projecthaystack.io;

import org.projecthaystack.HGrid;
import java.io.InputStream;

/** STUB: Haystack lib not bundled in extraction (Bucket-D dependency) */
public class HZincReader {
    public HZincReader(InputStream in) {}
    public HZincReader(String s) {}
    public HGrid readGrid() { return new HGrid(); }
}
